using System.IO;

namespace NesEmulator.RomMappers
{
    abstract class ROM
    {
        public static ROM Create(Stream stream)
        {
            // Parse. Determine mapper.

            return null;
        }
    }
}
